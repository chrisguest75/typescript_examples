{
  "name": "32_scripting_aws",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "32_scripting_aws",
  "projectType": "app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build:lib"],
      "options": {
        "command": "npm run build",
        "cwd": "32_scripting_aws"
      }
    }
  }
}
