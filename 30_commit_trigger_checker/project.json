{
  "name": "30_dockerignore_filter_commit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "30_commit_trigger_checker",
  "projectType": "app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build:lib"],
      "options": {
        "command": "npm run build",
        "cwd": "30_commit_trigger_checker"
      }
    }
  }
}
