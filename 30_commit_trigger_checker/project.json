{
  "name": "16_honeycomb",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "16_honeycomb",
  "projectType": "app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build:lib"],
      "options": {
        "command": "npm run build",
        "cwd": "16_honeycomb"
      }
    }
  }
}
