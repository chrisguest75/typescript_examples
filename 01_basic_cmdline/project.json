{
  "name": "01_basic_cmdline",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "01_basic_cmdline",
  "projectType": "app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build:lib"],
      "options": {
        "command": "npm run build",
        "cwd": "01_basic_cmdline"
      }
    }
  }
}
